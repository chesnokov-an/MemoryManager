# Лабораторная работа №3: Иерархия классов  
## Вариант N: Менеджер памяти  

### 📘 Описание проекта
Данный проект реализует **систему автоматического управления памятью**, выделяемой работающим программам.  
Физически память представлена **единым массивом фиксированного размера**, который используется всеми программами.  
Система поддерживает работу с различными типами элементов памяти, отслеживание ошибок и статистику использования.

---

### 🧩 Основные сущности

#### 1. Элемент памяти *(базовый класс)*
Абстрактный класс, описывающий общий интерфейс всех элементов памяти.  
Расширяем за счёт создания новых наследников (например, сегмент памяти внешнего устройства).

---

#### 2. Типы элементов памяти

##### 🔹 Одиночная переменная
**Описатель содержит:**
- Имя переменной  
- Размер выделенной памяти  
- Указатель на выделенную область  

##### 🔹 Массив
**Описатель содержит:**
- Имя массива  
- Общий размер выделенной памяти  
- Размер одного элемента  
- Указатель на выделенную область  

##### 🔹 Разделяемый сегмент памяти
**Описатель содержит:**
- Имя сегмента  
- Размер выделенной памяти  
- Размер элемента  
- Указатель на выделенную область  
- Список программ, имеющих доступ к сегменту  

##### 🔹 Ссылка
**Описатель содержит:**
- Имя ссылки  
- Указатель на другую переменную той же программы  
  или на общий разделяемый сегмент, объявленный в другой программе  

---

### 🧠 Логическая структура

#### Контейнер программы
Хранит данные обо всех выделенных участках памяти, принадлежащих конкретной программе.  
Обеспечивает **доступ к памяти по именам переменных**.

#### Таблица системы
Хранит данные обо всех зарегистрированных программах:
- Путь к исполнимому файлу  
- Квота памяти (максимальный объём, который может запросить программа)  
- Список данных о выделенной памяти  

Шаблонный класс таблицы реализуется как **хэш-таблица с перемешиванием сцеплением**.

---

### ⚙️ Основные функции системы

#### Для переменной:
- Получение значения  
- Изменение значения  
- Создание ссылки на переменную  

#### Для массива и разделяемого сегмента:
- Получение значения элемента по индексу  
- Получение значений в диапазоне индексов  
- Запись нового значения  

#### Для разделяемого сегмента:
- Предоставление доступа программе  
- Отказ программы от работы с сегментом  

#### Для программы:
- Выделить память указанного типа  
- Освободить память  
- Получить доступ к существующему разделяемому сегменту  
- Показать всю информацию об используемой памяти  
- Показать данные о разделяемых сегментах  
- Рассчитать общий объём используемой памяти \*

\* Указанная операция реализована в многопоточном режиме.
Каждая группа элементов памяти обрабатывается **в отдельном потоке**.

#### Для всей системы:
- Вывести сводку использования памяти  
- Показать долю памяти каждой программы  
- Вывести ошибки работы с памятью (по программам и по системе)  
- Показать некорректные ссылки (на освобождённую память)  
- Провести **дефрагментацию памяти**

---

### ⚠️ Обработка ошибок
Система должна фиксировать и протоколировать следующие типы ошибок:
- Ошибка выхода за границы памяти  
- Утечка памяти (повторное выделение без освобождения)  
- Ошибка доступа по несуществующему идентификатору  
- Ошибка ссылки на освобождённую память  

Все ошибки сохраняются в **специальный список ошибок**.